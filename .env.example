# Database (docker-compose default)
DATABASE_URL=postgresql://atlas:atlas@localhost:15432/atlas_quant

# Binance — paper (demo.binance.com keys)
BINANCE_DEMO_API_KEY=
BINANCE_DEMO_API_SECRET=

# Binance — live (only when promotion gates pass)
BINANCE_LIVE_API_KEY=
BINANCE_LIVE_API_SECRET=

# Alerts (optional — entry/exit notifications)
# Create bot: message @BotFather on Telegram → /newbot
# Chat ID: message @userinfobot or forward a message to your bot
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
