#!/usr/bin/env sh
set -e

echo "Running Prettier..."
npm run format

echo "Running lint..."
npm run lint

if ! git diff --quiet; then
  echo "Prettier updated files. Review and stage the formatting changes before committing."
  exit 1
fi
